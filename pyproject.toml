[project]
name = "test-jet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.4",
    "gunicorn>=25.3.0",
    "pytest>=9.0.3",
    "pytest-django>=4.12.0",
    "whitenoise>=6.12.0",
]
